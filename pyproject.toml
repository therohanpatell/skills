# Run directly with `python run.py`; no build backend or installation is needed.

[project]
name = "dtf-test-gen"
version = "0.1.0"
description = "Local, AI-assisted generator of minimal transformation-aware BigQuery test data for DTF pipelines"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

